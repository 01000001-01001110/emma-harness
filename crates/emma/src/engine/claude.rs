//! The claude engine: one goal, handed whole to the `claude` CLI.
//!
//! **A stub, declared ahead of its port from the macOS fork.** On Windows the
//! CLI is an npm shim (`claude.cmd`); `usertools.rs` has the resolution and
//! `CreateProcessW` precedents the port cites.
