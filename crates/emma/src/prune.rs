//! Dropping superseded tool results from the history that goes back to the
//! model.
//!
//! **Off unless asked for, and it stays off until its own measurement is
//! reproduced here.** The fork this is ported from claims 81.8% of byte-turns
//! across 25 recorded runs were carried by superseded `Read` results. That
//! figure is not verified in this repository — the recordings were not in the
//! archive — and this project's rule is that a number nobody measured here is
//! a number that does not go in a doc as fact.

// Port in progress. See `memory/mod.rs` for the note on why these stubs exist.
