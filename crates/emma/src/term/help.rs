//! Help: the text, once, and the page that draws it.
//!
//! **A stub, declared ahead of its port from the macOS fork.** `Ctrl+/`
//! anywhere, `?` on an empty box, and `/help` open it; the Help page sits on
//! top of every other page, which is why it is first in
//! `input::run_page_key`.
