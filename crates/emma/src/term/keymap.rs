//! The rebindable chord layer: `~/.emma/keybindings.json`.
//!
//! **A stub, declared ahead of its port from the macOS fork.** The derived
//! quick-help labels in `bindings.rs` are tested against the default chords;
//! the port extends that test to a non-default keymap so a rebound chord
//! cannot leave a stale label on screen.
