//! The bridge between the Code page's editor and the language servers the
//! model's tools already run.
//!
//! **A stub, declared ahead of its port from the macOS fork**, and blocked on
//! the multi-language `tools/lsp` port: it needs a per-language pool and four
//! client methods that do not exist here yet. Do not stub a second language
//! table in this crate to unblock it.
