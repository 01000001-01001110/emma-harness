> **Superseded, 2026-08-16.** The documentation is now the HTML site rooted at
> [`docs/index.html`](index.html), which is generated from the code rather than
> written alongside it and is the record `CLAUDE.md` treats as authoritative.
> This file is kept only until its remaining content is ported, and it is known
> to be wrong in at least one place: it was last revised on 2026-08-10 and the
> codebase has moved. Do not cite it.

